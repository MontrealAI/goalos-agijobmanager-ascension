# Public Trust QA

Public trust is checked through route integrity, metadata integrity, accessibility static checks, public-safe static scans, docs link checks, claim-boundary scans, dynamic tests, and post-build kernels.

Primary commands: `npm test` and `npm run build`.
