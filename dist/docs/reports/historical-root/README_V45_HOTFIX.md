# V45 Hotfix Overlay

This overlay fixes missing-file GitHub Actions failures by replacing individual test-file calls with guarded dynamic runners.

Use it when GitHub Actions fails with a missing `tests/repository-public-trust-v43.test.mjs` module.
