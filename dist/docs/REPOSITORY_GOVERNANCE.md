# Repository Governance

The repository preserves historical artifacts, avoids root clutter, uses one canonical route manifest, and keeps compatibility through committed adapters and dynamic guarded runners. Public copy must remain claim-bounded and data-zero.
